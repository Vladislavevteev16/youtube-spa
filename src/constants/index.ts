export const enum LanguageOptions {
  HEADER = "header",
  SEARCH_BAR = "searchBar",
  MODAL = "modal",
  SAVED_QUERY = "savedQuery",
}

export const enum ViewModeOptions {
  LIST = "list",
  GRID = "grid",
}

export const enum PairsOfValues {
  RECENT_REQUEST = "requests",
  LANGUAGE = "currentLanguage",
  SAVED_QUERY = "queries",
  THEME = "theme",
  AUTH = "token",
  VIDEOS = "items",
  MODAL = "modal",
}

export const enum YoutubeOrderValue {
  DATE = "date",
  RATING = "rating",
  RELEVANCE = "relevance",
  TITLE = "title",
  VIEW_COUNT = "viewCount",
}
